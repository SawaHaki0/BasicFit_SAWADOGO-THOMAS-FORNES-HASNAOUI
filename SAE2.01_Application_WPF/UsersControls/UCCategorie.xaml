<UserControl x:Class="SAE2._01_Application_WPF.UsersControls.UCCategorie"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SAE2._01_Application_WPF.UsersControls"
             xmlns:model="clr-namespace:SAE2._01_Application_WPF.Classes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#F8FAFC">

    <UserControl.DataContext>
        <model:ObservableCollections x:Name="Collections"/>
    </UserControl.DataContext>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Titre -->
            <RowDefinition Height="*"/>
            <!-- Contenu -->
        </Grid.RowDefinitions>

        <!-- TITRE -->
        <TextBlock Grid.Row="0" Text="Gérer catégorie" FontSize="32" FontWeight="Bold" 
                   Foreground="#0E2235" Margin="0,0,0,20"/>

        <!-- CONTENU : Table + Boutons -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <!-- DATAGRID -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" 
                    BorderBrush="#D9DEE5" BorderThickness="1" ClipToBounds="True">
                <DataGrid x:Name="dgCategorie" ItemsSource="{Binding LesCategories}"
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                          HeadersVisibility="Column" GridLinesVisibility="All"
                          HorizontalGridLinesBrush="#7CC2E8" VerticalGridLinesBrush="#7CC2E8"
                          Background="White" RowBackground="White" AlternatingRowBackground="White"
                          BorderThickness="0" RowHeight="45" ColumnHeaderHeight="38"
                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#E5ECFB"/>
                            <Setter Property="Foreground" Value="#0E2235"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="4"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Foreground" Value="#0E2235"/>
                            <Setter Property="Padding" Value="4"/>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="CATEGORIE" Binding="{Binding NomCategorie}" Width="*"/>
                        <DataGridTextColumn Header="DESCRIPTION CATEGORIE" Binding="{Binding DescriptionCategorie}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- BOUTONS -->
            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Top" Margin="20,0,0,0">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#F26B1A"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Height" Value="50"/>
                        <Setter Property="Margin" Value="0,0,0,15"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </StackPanel.Resources>

                <Button Content="Ajouter une catégorie"   x:Name="butAjouterCate"   Click="butAjouterCate_Click"/>
                <Button Content="Modifier une catégorie"  x:Name="butModifierCate"  Click="butModifierCate_Click"/>
               
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>